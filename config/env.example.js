var env = {
  port: 10011,
  host: '0.0.0.0',
  frontendTarget: 'http://localhost:4200',
  // followings are for fakedb (json-server)
  delay: [200, 500],
  fakedbPort: 10012,
};

module.exports = env;
